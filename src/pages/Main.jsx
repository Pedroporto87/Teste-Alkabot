import React from 'react';
import { BrowserRoute, Routes, Route } from 'react-router-dom';

export const main = () => {
  return (
    <div>
        <button>Posts</button>
        <button>Usuários</button>
    </div>
  )
}
